server:
  appName: keeper-panda
  name: keeper_panda
  port: 0
  ip: host.docker.internal
  endpoint:
    main: api/v1/panda
  eureka:
    enabled: true
    hostname: host.docker.internal
    prefer-ip: true
  db:
    host: keeper-postgres
    port: 5432
    name: keeper-postgres-db
    schema: keeper_panda
    user: ditar
    password: keeperAdmin1984
  logging:
    level: info
    filePath: ../logs/panda/panda.log
  pg:
    pattern: Aaaaaaaaaa00000
